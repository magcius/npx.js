// Contains actor scripting, which for this demo is individual physics logic.

(function(exports) {
    "use strict";

    // Controls the tilt for a single axis.
    var TiltPhysicsController = new Class({
        Name: 'TiltPhysicsController',

        initialize: function() {

        },
    });

    var TiltBridgeActor = new Class({
        Name: 'TiltBridgeActor',
    });

})(window);
